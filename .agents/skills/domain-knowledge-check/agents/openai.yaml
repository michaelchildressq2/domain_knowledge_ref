interface:
  display_name: "Domain Knowledge Check"
  short_description: "Grounded pattern alignment reviews"
  default_prompt: "Use $domain-knowledge-check to review this design against local subject-group patterns and return grounded alignment scores."

policy:
  allow_implicit_invocation: true
