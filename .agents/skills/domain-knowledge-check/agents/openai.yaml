interface:
  display_name: "Domain Knowledge Check"
  short_description: "Knowledge index for domain specific problems"
  default_prompt: "Use $domain-knowlowdge-check to review this input and search the ./subject-groups for relevant reference primciples and patterns. Make suggestions grounded by the relevant patterns."

policy:
  allow_implicit_invocation: true
